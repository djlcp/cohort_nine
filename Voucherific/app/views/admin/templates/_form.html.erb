<div class="section">
<%= simple_form_for [:admin, @template] do |f| %>
<div class="container">
<div class="card-deck mb-3 text-center">
  <div class="card mb-4 shadow-sm">
    <div class="card-header">
      <h4 class="my-0 font-weight-normal"> Create a template </h4>
    </div>
    <div class="card-body">
     <div class="field">
      <div class="control">
      <%= f.label :Value %><br>
       £ <%= f.number_field :value, input_html: { class: 'input' }, wrapper: false, label_html: { class: 'label' } %>
     </div>
    </div><br>
    <div class="field">
      <div class="control">
        <%= f.input :description, input_html: { class: 'textarea' }, wrapper: false, label_html: { class: 'label' }  %>
     </div>
     <br>
      <%= f.button :submit, 'Save template',class: "btn btn-success" %>
      <%= link_to "Back to templates list", admin_templates_path, class: "btn btn-success" %>
      <% end %>
    </div>