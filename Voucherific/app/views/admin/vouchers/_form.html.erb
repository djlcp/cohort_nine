</br>

<div class="section">
<%= simple_form_for [:admin, @voucher] do |f| %>
  <div class="field">
    <div class="control">
      <%= f.input :value, input_html: { class: 'input' }, wrapper: false, label_html: { class: 'label' } %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <%= f.input :description, input_html: { class: 'textarea' }, wrapper: false, label_html: { class: 'label' }  %>
    </div>
  </div>
  <%= f.button :submit, 'Save voucher',class: "btn btn-primary" %>
  <%= link_to "Back to vouchers list", admin_vouchers_path, class: "btn btn-primary" %>
<% end %>
</div>